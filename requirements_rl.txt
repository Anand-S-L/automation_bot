# Requirements for RL Farming Agent - State-of-the-Art Implementation

# Deep Learning
torch>=2.0.0
torchvision>=0.15.0

# Already installed (from previous setup)
opencv-python>=4.8.0
numpy>=1.24.0
mss>=9.0.0
pyautogui>=0.9.54
pillow>=10.0.0

# OCR for reward detection (CRITICAL for Phase 2)
easyocr>=1.7.0  # Better accuracy, slower
pytesseract>=0.3.10  # Faster, requires Tesseract installed

# Monitoring & Training Visualization (ESSENTIAL)
tensorboard>=2.14.0  # TensorBoard for training metrics
wandb>=0.15.0  # Weights & Biases (optional, for cloud tracking)

# Utilities
tqdm>=4.66.0  # Progress bars
matplotlib>=3.7.0  # Plotting
